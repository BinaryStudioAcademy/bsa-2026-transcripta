# Built into the production SPA bundle (vite build, mode=production).
# In production the backend serves the SPA and the API on the same origin,
# so the API base is just the path — no proxy, no absolute host.
VITE_APP_NODE_ENV=production
VITE_APP_API_ORIGIN_URL=/api/v1
